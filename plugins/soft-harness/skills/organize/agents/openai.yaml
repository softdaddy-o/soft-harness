interface:
  display_name: "Harness Organize"
  short_description: "Organize live host configs, memory partitioning, and .harness snapshot state."
  default_prompt: "Use $organize to change the real host configs, partition Claude/Codex memory when requested, catch config issues, treat missing .harness as normal, and refresh or initialize the .harness snapshot afterward."
