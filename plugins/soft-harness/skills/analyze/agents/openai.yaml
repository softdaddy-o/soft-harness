interface:
  display_name: "Harness Analyze"
  short_description: "Inspect current host state, memory partition candidates, and .harness snapshot health."
  default_prompt: "Use $analyze to inspect this repo, explain the current shared-vs-host-local and Claude/Codex memory-partition state, treat missing .harness as normal, and refresh or initialize the .harness snapshot without mutating host files."
