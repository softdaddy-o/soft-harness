version: 0

defaults:
  guides_root: ./guides

capabilities: []

guides:
  shared:
    - path: common/project.md
      scope: project
  claude:
    - path: claude/memory.md
      scope: project
  codex:
    - path: codex/coding.md
      scope: project

outputs:
  - id: project-codex
    preset: project-codex-stub
  - id: project-claude
    preset: project-claude-stub
